# Screencast checklist

1. Open WSL/PowerShell and run `openclaw gateway status`.
2. Show that Gateway is running on `127.0.0.1:18789`.
3. Open Telegram chat with `@Test_Business4_bot`.
4. Send a message and show the bot reply, or show the current OpenAI quota error if the provider limit is active.
5. Say briefly: OpenClaw was installed in WSL2, Node.js 24 was used, Telegram was connected through BotFather, OpenAI was configured as the AI provider.
6. Mention difficulties honestly: Gemini was unavailable because of region restrictions; OpenAI later returned quota limits.

Submission requires:

- Google Drive link with screencast.
- Telegram bot username: `@Test_Business4_bot`.
- Short text description from `README.md`.
